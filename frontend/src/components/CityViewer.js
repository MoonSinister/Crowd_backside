/**
 * components/CityViewer.vue
 *
 * Three.js 3-D city visualisation showing agent positions as coloured spheres
 * on a flat city grid, updated in real time as trajectory data is loaded.
 *
 * Props:
 *   trajectories: Array<{agent_id, lat, lon, intent_category, time_slot, day}>
 *   cityBounds: { lat_min, lat_max, lon_min, lon_max }
 */
